# GHCP Harness — 전역 규칙 (단일 always-on)

이 저장소는 GitHub Copilot 코딩 에이전트 하네스의 **설계와 구현**이다. 설계 근거는 [docs/](../docs/)에 있다.
이 파일은 **항상 로드**되므로 **변하지 않는 최소 불변 규칙**만 담는다. 계획·진행상태 같은 휘발성 메모는 여기 넣지 않는다(세션·PR·스크래치로).

## 불변 규칙 (모드 무관)

1. **서브에이전트는 read-only 전용.** 코드 작성·편집은 절대 위임하지 않는다. 메인 단일 스레드가 쓰기를 독점한다. (근거: docs/03 C3 — Cognition·Anthropic)
2. **모델 라우팅은 작업 경계에서만.** 한 작업 도중 모델을 바꾸지 않는다. 전환 시 핵심 컨텍스트를 명시적으로 핸드오프한다. (C4)
3. **load-bearing 컨텍스트는 요약·압축하지 않는다.** 현재 diff, 최근 테스트/에러 출력, 활성 파일, 현재 계획은 보존한다. 과거 탐색 로그만 압축 대상. (C2)
4. **검증 루프엔 상한이 있다.** 수정 전 기준선(테스트·진단 1회)을 잡고, 내 변경이 유발한 **신규 실패만** 고친다. 같은 실패 자가수정은 **최대 2회**, 그 후엔 동일 접근 반복을 멈추고 로그·diff·가설을 동봉해 **에스컬레이션**한다. 기존(pre-existing) 실패는 건드리지 않고 기록만 한다. (C8)
5. **커밋은 검증 통과 마일스톤에서만.** green 상태가 곧 롤백 지점이다. (C9)
6. **위험 등급제.** 가역·저영향(파일 편집, 읽기, 테스트 실행)은 자율 진행. **비가역·공유 영향**(push, `--force`, `reset --hard`, 파일/브랜치 삭제, 인프라 변경, 비밀 취급)은 항상 사용자 승인. (C1)

## 검증의 정의 (완료 기준)

"컴파일된다"가 아니라 **(1) 변경을 diff로 명확히 하고 (2) 관련 테스트를 실제로 돌려 통과**해야 완료다. 테스트가 없으면 `재현 → 수정 → 회귀 방지 테스트 추가`. (docs/02 §3.6)

검증 명령은 **락파일로 추론**한다(`pnpm-lock.yaml`→pnpm, `yarn.lock`→yarn, `package-lock.json`→npm). 명령을 모르면 추측하지 말고 확인·기록한다. 이 저장소 자체는 빌드·테스트 러너가 없는 **문서·설계 레포**이므로, 문서 변경은 링크·표 포맷·명령 일치로 검증한다.

## 도구 사용

- 검색은 `search`로 시작해 넓게 → 점진적으로 좁힌다. 읽기는 넓은 범위 한 번(`read`), 작은 read 반복 금지.
- 편집은 다중 편집 시 일괄 적용 도구 우선. 터미널(`execute`)은 한 번에 하나, 병렬 금지.
- 다단계 작업은 `todo`로 추적하고 한 번에 하나만 in-progress.

## 소통

- 간결하게. 파일 참조는 마크다운 링크로(백틱 금지). 한국어로 답한다.
